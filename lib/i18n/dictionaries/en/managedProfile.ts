/**
 * Managed profiles: a partner drafting a profile on someone else's behalf,
 * the claim flow that hands it to its real owner, and the owner's review and
 * access-grant screens. components/managed/*, app/claim-profile/[token],
 * app/user/managed-drafts/*, app/user/profile/{access,managed-review}/*,
 * app/partner/clients/*, and their API routes under app/api/managed-profile/
 * and app/api/profile/access.
 */
const managedProfile: Record<string, string> = {};

export default managedProfile;

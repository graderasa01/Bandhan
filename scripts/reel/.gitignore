# Regenerated by the build scripts. art/ and vo/ are NOT: they are the source
# images and voice takes the cut is made from, and nothing here can reproduce
# them, so both stay tracked.
node_modules/
package.json
package-lock.json
frames/
clips/
*.mp4
backends/
lipsync.json
talk/
narrator/
adclips/
adframes/
pages/
story/

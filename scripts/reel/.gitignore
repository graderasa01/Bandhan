# Everything here is regenerated by the build scripts — art/ is NOT, it is
# the source images the story cut is made from, so it stays tracked.
node_modules/
package.json
package-lock.json
frames/
clips/
*.mp4
backends/
lipsync.json
talk/
narrator/
adclips/
adframes/
pages/
vo/
story/
